# L0 modeling source

This source deliberately contains no target ontology and no hidden answer. The
modeling agent must return this fixed non-answer description:

`Candidate describes a bounded business concept and records unresolved facts explicitly; no platform write is requested.`
