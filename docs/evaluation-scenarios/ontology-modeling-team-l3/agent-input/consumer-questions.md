# Required consumer outcomes

The model must let a consumer determine the concrete published C behavior B currently
consumes, whether historical/current outputs represent continuous business meaning, and
what B is known to do when C has no score. Answers come from public semantic facts; do
not invent an unconfirmed value.
