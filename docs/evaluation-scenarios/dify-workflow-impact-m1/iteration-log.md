# M1 iteration log

| Iteration | Hypothesis and model choice | Test result | Limitation | Next candidate |
| --- | --- | --- | --- | --- |
| 1 | Model call sites as `ToolInvocation` on `WorkflowVersion`; recover transitive callers with a SPARQL property path rather than materializing a domain dependency path. Publication is a controlled state literal. | Offline source hashing, RDF parse, SHACL positive/negative, supported RDFS subclass entailment, published C->B->A context, and draft isolation are executable in `tests/test_scenario.py`. | The development reasoner performs only limited RDFS. The fixtures have complete information and do not yet exercise an explicit unknown boundary. No owned managed workspace has been populated. | Add an isolated explicit-gap/unknown fixture only when a consumer-query boundary needs it; run documented governed runtime acceptance against a fresh owned ontology. |
| 2 | Correction after independent Round 1: preserve the earlier record, but add C's current-version `content:string` input, B-upstream -> C-input binding, `VariableUse producesVariable` for the IF/ELSE result, stable call-site ID/location, and ChangeSet before/after version links. Require direct `WorkflowVersion` type in fixtures for data-only SHACL; retain RDFS only as `producesVariable rdfs:subPropertyOf derivedFromVariable`. | Added exact-link context query plus remove-and-swap anti-cartesian mutations for all C->B->A links. The isolated explicit-gap fixture already exists and requires `unknownDetail`; it corrects Iteration 1's now-stale limitation without rewriting history. | Runtime acceptance remains subject to the active canonical-writer `legacy_only` gate and must use a fresh owned workspace; no raw loader or validation bypass is acceptable. | Retry governed semantic edits with `validate=true`, then managed validation/reasoning/scoped SPARQL, and record only exact owned-runtime evidence. |
| 3 | Fresh owned runtime acceptance used the governed generic semantic-edit path, not `datasets:load`: ontology, Shapes, and the repaired published Fixture were each submitted with `validate=true`; the Fixture used the Shapes graph. | Project `e94ca327-284d-4453-a8d3-7ac1d98ff193`, Ontology `16cfa560-fd29-4c0a-b5f1-1fcca25181d4`; edit audits `759a32f7-cf9f-42d4-a25f-5efd3fa8932d`, `38c8cb8c-4470-4527-8907-0f78d4d1e334`, `cf74898f-c09f-492a-8a8e-d04ec65196ae`. Managed validation succeeded/conformed, reasoning succeeded/consistent, and scoped published-caller SPARQL returned A and B. Cleanup GET returned 404 after deletion. | `GET /api/semantic/canonical-mode` still reports `product_write_mode: legacy_only`; Modeling Batch's canonical writer remains unavailable, although the governed generic edit path works. | Independent Round 2 may validate the repaired package and runtime evidence; do not treat Modeling Batch as exercised unless product mode changes. |
| 4 | Correct TBox contradiction found by Independent Round 2: introduce `referencesVariable` (`VariableUse -> Variable`) and make `usesVariable` and `producesVariable` its subproperties. Keep `derivedFromVariable` exclusively `Variable -> Variable`. | The development reasoner now checks `producesVariable -> referencesVariable`; an independent `owlrl.RDFS_Semantics` closure asserts that the production edge infers `referencesVariable` but never types `b-if-use` as `Variable`, while retaining `VariableUse`. Existing exact-link and mutation checks remain unchanged. | This is a TBox/offline inference correction only; it does not change asserted fixture topology, Shapes, or runtime write mechanics. | Run Independent Round 3; reuse Round 2 governed runtime evidence unless a reviewer identifies a write-contract effect. |
| 5 | Platform visualization check found that the generic entity read model anchors identity on `owl:NamedIndividual`. Add standard named-individual declarations and `rdfs:label` annotations to the published Fixture without changing its business relations. | Scenario acceptance adds a visualization regression; the managed entity read model can now return labeled C/B/A workflows, versions, invocations, bindings, variables, uses, Tools, and the Change Set. | Draft, invalid, and explicit-gap Fixtures remain test-oriented isolated graphs and are not loaded into the visualization Project. | Use the persistent platform Project to review graph readability before deciding whether a later iteration needs a smaller projection or additional labels. |

## Independent test rounds

### Round 1 — 2026-07-24 — FAIL

- Workspace/state: independent test of the uncommitted M1 scenario pack; no product code was changed.
- Executed offline acceptance: `uv run --directory backend python ../docs/evaluation-scenarios/dify-workflow-impact-m1/tests/test_scenario.py` — **9/9 passed**. This covers offline source hashes, pinned commit/license, RDF parsing, SHACL positive/draft/explicit-gap and invalid rejection, the limited RDFS subclass entailment, published callers, and draft/latest separation.
- Focused adjacent regression: `uv run --directory backend pytest tests/test_modeling_batches_service.py tests/test_semantic_validation.py tests/test_semantic_reasoning.py tests/test_semantic_context_query_api.py` — **69 passed** (5 deprecation warnings).
- Source/provenance and platform-boundary review: PASS. `manifest.json` separates official source, synthetic fixture, and Agent/query inference; repository diff has no Dify-named code under `backend/app` (one pre-existing frontend placeholder only).
- **HIGH — required C -> B data-use path is not represented.** `fixtures/published-deletion.ttl` has no C `content:string` input. `b-content-binding` is `B.content -> B.content`, rather than B's upstream variable -> C input; `B`'s IF/ELSE use and `approved_content` output have no production/derivation edge. `published-context.rq` independently joins a B IF use and a B output use (lines 7-18), so its one-row result cannot prove that the IF/ELSE produces the output. Repair requirement: add typed C input and cross-version binding, add an explicit production/derivation relation from the IF/ELSE result to `approved_content`, constrain/query each link, and add negative anti-cartesian assertions.
- **HIGH — governed runtime acceptance is blocked, not merely undocumented.** Fresh owned Project `6316f35c-5891-4bce-922f-826c215b627f` / Ontology `33810ca8-1448-4253-b4ae-5a4339f9ee51` was created with an Evidence Reference and Build Session. A real `dry_run` Modeling Batch (`create_class`, linked Evidence) returned `validation_failed` / `candidate_validation_failed`: `Canonical writer is not enabled in this mode; SEMANTIC_PRODUCT_WRITE_MODE=legacy_only`. This confirms the documented Modeling Batch limitation is real in the active runtime and is stronger than an arbitrary-RDF expressiveness limitation.
- Runtime governed-edit evidence: ontology Turtle and Shapes were accepted through `POST /api/semantic/edits` (audit IDs `0e43829f-de0b-4e8f-b8a5-237e35a43d3b` and `a0acb04c-1b38-44cc-bb0e-8b6e511acb33`); the manifest Evidence Reference was associated with the ontology edit (`ea8eccdf-2630-4e35-93cb-40b7e7dda0b8`). The required published Fixture write to the managed `asserted_data` graph with `validate=true` was rejected: `Semantic edit candidate does not conform to SHACL shapes`. Reproduction with only fixture data plus Shapes reports one precise violation: `published-delete-quality` -> `changeAppliesToVersion` -> `c-v2` does not have direct class `WorkflowVersion`; its `PublishedWorkflowVersion rdfs:subClassOf WorkflowVersion` axiom is in the separate ontology graph. The subsequent managed validation/reasoning runs failed because the rejected write left no physical data graph, and scoped SPARQL correctly returned no callers. Do not bypass this by using `datasets:load` or `validate=false` as acceptance.
- Cleanup: ownership was verified by the unique name/description, then `DELETE /api/projects/6316f35c-5891-4bce-922f-826c215b627f` returned `204`; follow-up `GET` returned `404`.
- Unexecuted as PASS: real managed validation, configured reasoning and scoped published-query acceptance cannot pass until the Fixture/model repair and a governed cross-graph validation/application route (or explicitly accepted generic runtime configuration) are available.
- Conclusion: request repair and then run Round 2 using this same scenario/log. Do not claim M1 acceptance from the offline 9-test result.

### Round 2 — 2026-07-24 — FAIL

- Workspace/state: independently retested the repaired uncommitted scenario package. Round 1 remains unchanged; no product code or delivery record was modified by this test.
- Round 1 repairs: PASS. The published Fixture now has `C.content:string`, B's upstream-source -> C-input binding, IF/ELSE `producesVariable approved_content`, before/after ChangeSet versions, direct `WorkflowVersion` fixture types for data-only SHACL, and stable call-site identifiers. `published-context.rq` requires every link; its remove-and-swap mutation test covers nine critical links and prevents the previous cartesian-style success.
- Offline/provenance/constraint acceptance: `uv run --directory backend python ../docs/evaluation-scenarios/dify-workflow-impact-m1/tests/test_scenario.py` — **11/11 passed**. It verifies immutable hashes/commit/license/provenance layers, RDF parsing, positive/draft/explicit-gap SHACL isolation, invalid rejection, limited RDFS expectation, exact published/draft queries, completeness, and mutation coverage.
- Focused adjacent regression: `uv run --directory backend pytest tests/test_modeling_batches_service.py tests/test_semantic_validation.py tests/test_semantic_reasoning.py tests/test_semantic_context_query_api.py` — **69 passed** (5 deprecation warnings). No Dify-named code is present under `backend/app`.
- Fresh governed runtime acceptance: owned Project `eb54735a-5142-4697-af24-42ecb0fcac72` / Ontology `4d4f44cc-57c6-42b4-81d5-771a23616a46` accepted ontology, Shapes, and published Fixture through `POST /api/semantic/edits` with `validate=true` (audits `91438d57-d473-430c-8e7d-94c9ef8d2f21`, `90ee9498-95a3-49ac-a2d8-8bcbcca23d56`, `44a19c8d-ad51-4721-8ad7-c4ee9e667ce8`). Evidence association `e63c7898-d6b4-43b7-a50f-d70eae54c67e` linked the manifest Evidence to the fixture audit. Managed validation `469a0dc7-2123-4da2-831b-d06993fad189` succeeded/conformed; reasoning `dc8bf67e-7e96-4258-a7b8-fe1313612303` succeeded/consistent; scoped published callers returned exactly A and B, and scoped `published-context.rq` returned one complete C->B->A row. No raw loader or validation bypass was used.
- Residual runtime limitation (not the reason for this model FAIL): `GET /api/semantic/canonical-mode` returned `product_write_mode: legacy_only`; a real generic `create_class` Modeling Batch dry-run returned blocking `candidate_validation_failed` / `Canonical writer is not enabled in this mode; SEMANTIC_PRODUCT_WRITE_MODE=legacy_only`. The generic governed edit path above is nevertheless sufficient for this M1 runtime contract.
- **HIGH — standard RDFS semantic contradiction in the repaired TBox.** `producesVariable rdfs:subPropertyOf derivedFromVariable`, while `producesVariable` has domain `VariableUse` and `derivedFromVariable` has domain `Variable`. Standard RDFS first entails `b-if-use derivedFromVariable b-approved-content`, then applies the superproperty domain and entails `b-if-use rdf:type Variable`. Independent `owlrl.RDFS_Semantics` closure confirmed all three facts, although `backend/scripts/dev_owl_reasoner.py` currently does not recurse domain/range over newly inferred subproperty triples and therefore hides it. This collapses the required distinction between a variable-use event and a variable, so it is a real model defect rather than style.
- Required repair: remove this incompatible subproperty relationship or replace its superproperty with a domain-compatible generic relation (for example, an unconstrained `relatedVariable`); retain `producesVariable` with `VariableUse -> Variable` domain/range. Update the limited-RDFS assertion and add a standard-RDFS regression assertion that `VariableUse` is **not** inferred as `Variable` from the production edge.
- Cleanup: ownership was verified from the unique Project name/description. `DELETE /api/projects/eb54735a-5142-4697-af24-42ecb0fcac72` returned `204`; follow-up `GET` returned `404`.
- Conclusion: request the narrow ontology/test repair, then run Round 3 using this same log. Runtime acceptance is otherwise PASS; do not mark M1 PASS until standard RDFS preserves the concept boundary.

### Round 3 — 2026-07-24 — PASS

- Workspace/state: independently tested the narrow TBox repair; Rounds 1 and 2 remain unchanged. No product code or delivery record was modified.
- Round 2 semantic repair: PASS. `referencesVariable` is `VariableUse -> Variable`; both `usesVariable` and `producesVariable` are its subproperties; `derivedFromVariable` remains separately `Variable -> Variable`. The development reasoner emitted only compatible `referencesVariable` subproperty assertions for `b-if-use` (from the asserted `usesVariable` and `producesVariable` edges), never `derivedFromVariable`. Independent standard `owlrl.RDFS_Semantics` closure confirmed `referencesVariable` and `VariableUse`, and confirmed that `b-if-use` is **not** inferred as `Variable`.
- Offline/provenance/constraint/query acceptance: `uv run --directory backend python ../docs/evaluation-scenarios/dify-workflow-impact-m1/tests/test_scenario.py` — **12/12 passed**. This includes source hashes/commit/license/provenance separation, RDF parse, SHACL positive/draft/explicit-gap/invalid isolation, limited and standard RDFS checks, exact published/draft queries, completeness, and nine-link remove-and-swap mutation coverage.
- Focused adjacent regression: `uv run --directory backend pytest tests/test_modeling_batches_service.py tests/test_semantic_validation.py tests/test_semantic_reasoning.py tests/test_semantic_context_query_api.py` — **69 passed** (5 deprecation warnings). `git diff --check` passed; no Dify-specific code was found under `backend/app`.
- Runtime decision/evidence: a new live run was necessary because the changed TBox is supplied to managed reasoning, so Round 2 runtime evidence alone could not validate it. Fresh owned Project `a8fb64f6-29d5-4782-ba47-020771c08e5d` / Ontology `118696fd-3b69-415a-b00f-b0dfc1a40f8a` applied ontology, Shapes and published Fixture through governed `POST /api/semantic/edits` with `validate=true` (audits `995cccd5-4003-4d8f-8696-14cdf7558898`, `397e2e03-796f-4869-a813-e87a76b598e4`, `15d02fe5-7791-41c5-b93e-3cf5a3c8d1d5`). Evidence association `b5214a87-3b12-46b6-a78c-3258a7bb898a` linked the manifest reference to the fixture edit. Managed validation `69c250a8-bd7b-4684-935e-050617c96a19` succeeded/conformed; reasoning `f592162a-eb04-4aa9-bf35-3f4abf93f232` succeeded/consistent; scoped `published-context.rq` returned exactly one complete row including B `approved_content` and A downstream use. No raw loader or validation bypass was used.
- Residual generic limitation: the live mode remains `canonical_store=legacy`, `product_write_mode=legacy_only`, and `read_mode=legacy`. Round 2 already independently reproduced the blocking Modeling Batch dry-run. This does not fail M1 because the governed generic semantic-edit path above completes the required validation/reasoning/scoped-query contract.
- Cleanup: unique ownership was verified before deletion. `DELETE /api/projects/a8fb64f6-29d5-4782-ba47-020771c08e5d` returned `204`; follow-up `GET` returned `404`.
- Unexecuted cases: none within the agreed M1 scope. The runtime exercised the published positive path; draft/invalid/explicit-gap remain intentionally isolated and fully covered by the offline executable suite.
- Conclusion: M1 first candidate passes its current minimal acceptance contract. It remains an iterative candidate, not a claim that the long-term ontology or Modeling Batch product mode is finalized.
