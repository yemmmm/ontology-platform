interface:
  display_name: "Ontology Builder"
  short_description: "Run staged, evidence-backed ontology modeling"
  default_prompt: "Use $ontology-builder to recover durable workflow state, organize business knowledge, model and independently review a vertical slice, then apply and verify it safely."
