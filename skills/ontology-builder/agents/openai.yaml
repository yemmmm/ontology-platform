interface:
  display_name: "Ontology Builder"
  short_description: "Build evidence-backed ontologies with recoverable batches"
  default_prompt: "Use $ontology-builder to recover current platform state, clarify the domain, dry-run evidence-backed modeling batches, stop at write decisions, and verify results with context and lineage."
